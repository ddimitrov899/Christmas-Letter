import { RemoveStringDearSantaPipe } from './remove-string-dear-santa.pipe';

describe('RemoveStringDearSantaPipe', () => {
  it('create an instance', () => {
    const pipe = new RemoveStringDearSantaPipe();
    expect(pipe).toBeTruthy();
  });
});
